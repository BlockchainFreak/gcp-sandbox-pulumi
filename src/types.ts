export type ProjectDetails = {
    // Display Name Associated With The Project
    projectDisplayName: string;
    // Project ID
    projectId: string;
    // emails of the project owners
    owners: string[];
    // Budget Alert Amount
    budgetAmount: string;
};